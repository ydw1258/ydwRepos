trade-off
service level

DB
검색 등록 수정 제거
계층형 DB : 조직도, 전체 구조도
RDB(관계형) : 2차원 표방식 DB
객체지향 DB, XML DB : 
NoSQL DB : 

DBMS DB management System : db를 관리하는 소프트웨어
DB : DBMS를 포함한 OS, 추가 애플리케이션 등을 포함한 개념
APM 등

Oracle, MSSQL, MYSQL, PostgreSQL..

RDB 계층
인스턴스 메모리에 올라간 프로그램
DB 한개의 프로젝트
오브젝트 테이블, 프로시져 index등

//멀티바이트에서 한글 설정
mysql_query(&conn, "set session character_set_connection=euckr;");
mysql_query(&conn, "set session character_set_results=euckr;");
mysql_query(&conn, "set session character_set_client=euckr;");

transition세팅(10/16)
acid
auto commit 끄기
start transaction;
--사이에 적용했던 내용들을 지움. (commit 하지 않고)
rollback;

SELECT문
COUNT, SUM, AVG, MAX, MIN
GROUP BY  ~HAVING

EX)
SELECT * FROM user_data ORDER BY MONEY DESC, ID DESC;

JOIN은 코스트가 큰 연산이라 NOSQL에서 줄이는 연산중 하나

현업에서는 프로시져 콜을 보통 사용직접sql문 거의 안씀.
